# Runtime dependencies (production).
# Pin lower bounds; allow patch/minor updates.
requests>=2.32,<3
python-dotenv>=1.0,<2
numpy>=1.26,<3
sentence-transformers>=3.0
faiss-cpu>=1.8
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
# Telegram bot (Module 7). Pinned to a major to allow security/feature updates
# within the same generation.
python-telegram-bot>=21.0,<23
